# Airflow User ID (use `id -u` on Linux)
AIRFLOW_UID=1000

# PostgreSQL Connection String
DATABASE_URL=postgresql://airflow:airflow@localhost:5432/virginia_court_cases

# DuckDB Database Path for Gold layer / Analytics UI
DUCKDB_PATH=output/gold/court_analytics.duckdb
